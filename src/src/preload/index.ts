/**
 * Preload script for Electron
 * Bridges renderer and main process
 */

// For now, settings.html uses node integration
// In production, you'd expose specific APIs here via contextBridge
// But for simplicity, we're using nodeIntegration: true in the settings window

export {};
